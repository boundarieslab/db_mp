# Local copies of the CDN libraries, so tools/smoke_test.js can run offline.
vendor/

# Hand-over folders and working rasters. Neither belongs in the published site.
* outputs/
*.tif

# Patch backups and scratch left by tooling.
*.bak
_to_delete/
